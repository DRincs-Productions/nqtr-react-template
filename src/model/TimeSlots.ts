export enum TimeSlotsEnumNumber {
    MORNING = 0,
    AFTERNOON = 1,
    EVENING = 2,
    NIGHT = 3
}

export interface IImageTimeSlots {
    morning: string
    afternoon: string
    evening: string
    night: string
}
